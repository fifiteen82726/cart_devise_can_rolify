<p id="notice"><%= notice %></p>

<div class="row col-md-4">
<table class="table table-hover">
  <tr><td><strong>品名：</strong></td>
    <td><%= @admin_product.name %></td>
  </tr>

  <tr><td><strong>說明：</strong></td>
    <td><%= @admin_product.description %></td>
  </tr>

  <tr><td><strong>單價：</strong></td>
    <td><%= @admin_product.price %></td>
  </tr>

  <tr><td><strong>庫存：</strong></td>
    <td><%= @admin_product.stock %></td>
  </tr>
  </table>
<%= link_to 'Edit', edit_admin_product_path(@admin_product), class: 'btn btn-xs btn-success' %> |
<%= link_to 'Back', admin_products_path, class: 'btn btn-xs btn-warning' %>
  </div>

