<%= form_for(@admin_product) do |f| %>
  <% if @admin_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_product.errors.count, "error") %> prohibited this admin_product from being saved:</h2>

      <ul>
      <% @admin_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, '品名' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description, '描述' %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :price, '價格' %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :stock, '庫存' %><br>
    <%= f.number_field :stock %>
  </div>
  <br>
    <%= f.submit '送出', class: 'btn btn-md btn-info' %>
<%= link_to '返回', admin_products_path, class: 'btn btn-md btn-warning' %>
<% end %>
